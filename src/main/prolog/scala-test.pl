% add(A, B) :-
%   integer(A),
%   integer(B).

% sub(A, B) :-
%   integer(A),
%   integer(B).

% mul(A, B) :-
%   integer(A),
%   integer(B).

% div(A, B) :-
%   integer(A),
%   integer(B).

% expression(A, B) :- mul(A, B), add(A,B), sub(A,B), div(A,B).

generate_integers.
